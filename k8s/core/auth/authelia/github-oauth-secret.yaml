apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: authelia-github-oauth
  namespace: authelia
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: onepassword-vault
    kind: ClusterSecretStore
  target:
    name: authelia-github-oauth
    creationPolicy: Owner
  data:
    - secretKey: client_id
      remoteRef:
        key: authelia-github-oauth
        property: client_id
    - secretKey: client_secret
      remoteRef:
        key: authelia-github-oauth
        property: client_secret
